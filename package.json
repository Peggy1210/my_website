{
  "name": "my-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "echo \"VITE_BUILD_TIME=$(date -u '+%Y-%m-%dT%H:%M:%SZ')\" > .env.local && vite build",
    "deploy": "gh-pages -d dist -b deploy",
    "preview": "vite preview"
  },
  "dependencies": {
    "dayjs": "^1.11.19",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.3",
    "gh-pages": "^6.3.0",
    "vite": "^6.3.5"
  }
}
